---
name: pet-chibi-studio
description: 将宠物照片制作成明显大眼、圆润大头、迷你身体、柔软毛发的萌化头像；支持单宠、多宠贴近合照，以及缩胸、调整距离、柔化毛感等定向编辑。用于“按这个模板做我家宠物”“大头小身体”“靠近一点”等有图像目标的请求，不用于普通宠物问答或医学判断。
---

# 宠物大头萌化工作室

新作的核心风格是明显大眼、夸张圆润大头和迷你身体，三者同时成立；保留身份不等于照搬原照的眼睛大小和正常头身比。眼色、眼形特点与个体辨识度保留，眼睛可以明显放大；避免凸眼只约束结构与质感，不限制大眼效果。已有作品的编辑与合照按本轮保持项执行。

默认交付实际图片。用户只要求修改 Skill 或提示词时，只修改文件，不自动生图。提示词和参考图是软约束，不承诺像素锁定或每次完全相同。

## 1. 确定本单与图片

- 直接查看用户图片，区分原照 P、用户指定/认可作品 A、姿态 T、比例 G、编辑底图 E 和失败候选 C，定义见 [固定提示词模板](references/prompt-recipes.md)。同宠多张照片归为同一主体。
- 多只动物且尚未明确出图方式时，先问“分别生成每只动物的单独图片，还是把多只动物放在同一张合照里？”用户已选则沿用；单宠默认 1 张，合照默认共 1 张，分别生成默认每只 1 张。不把多个版本变成四宫格。
- 用户明确指定的图优先，不能因文件更新就改用最新候选。只有图片缺失、归属不明或出图方式未确定时追问；其他按现有上下文推进。
- 优先当前原生图像工具，真实 schema 决定能力，不虚构蒙版、seed、模型参数或硬几何锁定。按需读 [运行说明](references/runtime-and-sources.md)。

## 2. 选模式并冻结本轮依据

| 模式 | 图片依据 | 比例与姿态规则 |
|---|---|---|
| N 新作 | P 只管身份，T/G 分别管姿态与比例 | 用统一新作目标；原照视角和身体尺寸不能改变模板 |
| E 定向编辑 | 用户指定 E，以及分项认可的 A；必要时 P/T/G 校对 | 仅改要求的部分，未要求改比例就保留 E/A 内部比例 |
| C 已选作品合照 | 每只对应的 A，必要时另加 C 指示合照布局 | 整只等比缩放和平移；各自姿势与内部头身比取自 A |

“再小一点”只属于本单本轮，不自动更新所有订单默认；“姿势不错”也不代表身体比例已被认可。分别记录用户明确认可的方面。通用数值不能推翻用户明确要求保留的作品比例。

每次调用必须读 [固定提示词模板](references/prompt-recipes.md)，按原文组装任务段、一个模式段、本单物种面貌段、视觉段、可选的当前修正段和末尾负面约束。只填字段，不每轮自由改写风格、换比例口径或堆叠历史指令。完整提示词和实际输入图顺序在调用前保存到订单目录。

首图和所有修正都保留本轮固定参照。合照每轮带回所有 A；失败图 C 仅负责指出偏差或已通过布局，不能成为唯一参考。工具容量不足不得静默丢图。修正时不更换已通过的表情、姿势或比例。

## 3. 只读需要的风格参考

新作读取 [风格规范](references/style-guide.md) 和 [比例控制](references/proportion-control.md)。猫姿态用 `assets/pose-cat-gray.png` 或 `assets/pose-cat-golden.png`，狗用 `assets/pose-dog.png`；比例可查看 `assets/template-original.jpg`、`assets/single-cat-round.png`、`assets/dog-proportion-approved.png`。实际查看后分配角色，不把旧参考误称为已达到当前夸张比例。

合照优先本单 A；没有已选作品、需要通用毛感示例时才查看 `assets/duo-soft.png`。仅诊断过锐时查看 `assets/duo-before-softening.png`。不要让通用风格图与已认可单宠图竞争。

新作猫狗：略侧坐，头向画面左歪、左眼低右眼高，脸和口鼻抬向右上，视线朝相机附近上方，相机轻微俯拍。鸟类等其他宠物用相应自然解剖，不强加猫狗坐姿；已有指定作品则保留其姿势。

保留本单毛色、花纹、眼鼻色、耳形、口鼻、毛长与可见特殊标记；通用正文不绑定具体毛色与品种，只在本单字段填写必要辨识点或颜色校正。逐只记录表情依据：新作取选定 P，编辑/合照取指定 E/A，闭嘴与张嘴不相互替换。猫保留鼻形与柔和脸颊，狗保留口鼻长度、厚度与毛流；E/C 未要求改脸或眼睛时，面貌段只校对身份，保留已选作品的眼睛大小和头身比例。用户明确说明的滤镜偏色需校正；不将蓝灰毛误画成黑毛，也不把真实金黄色毛漂白。不把未知品种、不可见细节当作已核实事实。

## 4. 执行、复核与停止

默认方形头像、近白中性浅灰不透明背景、柔光、完整耳脚，主体联合轮廓居中。内部比例与外部大小分开处理，胸段不能为了填满画幅而拉长。具体标准见风格规范，不在不同文件叠加多组比例数值。

结果出来后打开实际图片，执行 [验收清单](references/quality-check.md)。先区分“新作/改比例的目标检查”和“保留作品的相对检查”，再检查身份、姿态、毛感、布局与数量。合照逐只对照原始 A，不只检查站位。

失败按预算定向修正，默认每张首图加最多 2 次修正，通过即停。未指定错误不得预设总要出两张；用户明确次数优先。用完预算仍失败则如实报告，不能用含耳与不含耳两套测量换取通过，也不能暗中无限重试或切额外收费接口。工具预览可能即时出现，检查完成前不能宣称已验收。

订单记录使用 [状态模板](templates/order-state.yaml)，保留真实路径、固定参照、提示词版本、每轮实际输入与修正依据。仅在用户明确选中时更新认可状态。多客户任务才读 [批量规则](references/batch-workflow.md)。

可提交的 Skill 文件只使用相对路径或通用占位符，不记录使用者姓名、账号、邮箱、设备名、个人绝对路径或聊天来源。实际图片路径、测试输出与订单记录保存在仓库之外；不将个人资料加入公共模板。提交前检查文档、图片元数据与 Git 作者身份。
